# Visual quality evaluation fixtures

This directory reserves stable public-paper fixtures for the visual product evaluation stage. Each fixture should contain `input.txt`, `expected.json`, and a generated candidate manifest. The first P3 fixtures are the Chinese Agent loop, a RAG branch, and a multi-series benchmark CSV.
