# Draw.io M0 smoke prompt

Create an editable left-to-right Draw.io workflow using exactly the nodes and edges in `examples/m0/workflow.json`. Use native editable vertices, text labels, and edge cells. Do not add modules, claims, or decorative illustrations. Save the native `.drawio` source and export SVG/PDF with embedded diagram data when the Draw.io integration is available.
