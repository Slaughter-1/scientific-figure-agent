# Figma M0 smoke prompt

Using the shared `examples/m0/workflow.json`, create a native Figma frame named `M0 Agent Workflow`. Use editable Frames, Text nodes, Rectangles, Lines/Arrows, and groups with a left-to-right layout. Preserve exactly the nine labels and eight edges from the source. Do not rasterize the result. Export one SVG or PDF after inspecting the frame.

Current environment note: the official Figma MCP is not connected in this Codex session, so this prompt is ready for the manual smoke run when a Figma connection is available.
